version: '3.8'

services:
  mobile-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: diaba_mobile_web
    ports:
      - "8080:80"
    restart: always
    environment:
      - API_URL=http://localhost:8000/api
